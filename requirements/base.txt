# CPU-safe dependencies — installable on Mac (no CUDA).
# Install the CPU build of torch from the official index.
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.3.0
transformers>=4.40,<5.0
onnx>=1.16
numpy>=1.26,<2.0
pyyaml>=6.0
